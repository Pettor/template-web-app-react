{
  "name": "mock",
  "private": true,
  "version": "0.6.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "clean:all": "pnpm clean",
    "mocks": "mocks-server",
    "lint": "eslint ./mocks --ext .json",
    "lint:fix": "pnpm lint --fix",
    "test:e2e:env": "mocks-server --no-plugins.inquirerCli.enabled --log=silent"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*"
  }
}
