{
  "name": "mock",
  "private": true,
  "version": "0.5.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "clean:all": "pnpm clean",
    "mocks": "mocks-server",
    "lint": "eslint ./mocks --ext .json",
    "lint:fix": "pnpm lint --fix"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*"
  }
}
