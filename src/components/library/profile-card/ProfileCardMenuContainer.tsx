import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const ProfileCardMenuContainer = styled(Box)(({ theme }) => ({
  marginBottom: theme.spacing(2),
}));

export default ProfileCardMenuContainer;
