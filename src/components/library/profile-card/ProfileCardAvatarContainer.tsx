import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const ProfileCardAvatarContainer = styled(Box)(() => ({
  display: "flex",
  height: "100%",
  alignItems: "center",
}));

export default ProfileCardAvatarContainer;
