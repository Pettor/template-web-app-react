import Grid from "@mui/material/Grid";
import { styled } from "@mui/material/styles";

const HomeViewGridContent = styled(Grid)(({ theme }) => ({
  padding: theme.spacing(2),
}));

export default HomeViewGridContent;
