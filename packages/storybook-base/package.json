{
  "name": "storybook-base",
  "version": "0.6.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rimraf dist",
    "clean:all": "pnpm clean node_modules"
  },
  "devDependencies": {
    "eslint-config": "workspace:*",
    "react-utils": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
