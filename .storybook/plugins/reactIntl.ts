import en from "../../locale/compiled/en.json";

// Add locales here
const locales = ["en"];

const messages = {
  en: en,
};

export const reactIntl = {
  defaultLocale: "en",
  locales,
  messages,
};
